<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend_novel_review.user.repository.UserRepository">

    <resultMap id="UserResultMap" type="com.example.backend_novel_review.user.domain.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="role" column="role"/>
        <result property="provider" column="provider"/>
        <result property="providerId" column="provider_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByProviderAndProviderId" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users ORDER BY created_at DESC
    </select>

    <insert id="save" parameterType="com.example.backend_novel_review.user.domain.User">
        INSERT INTO users (email, nickname, profile_image_url, role, provider, provider_id)
        VALUES (#{email}, #{nickname}, #{profileImageUrl}, #{role}, #{provider}, #{providerId})
    </insert>

    <update id="update" parameterType="com.example.backend_novel_review.user.domain.User">
        UPDATE users
        SET nickname = #{nickname},
            profile_image_url = #{profileImageUrl}
        WHERE id = #{id}
    </update>

</mapper>
