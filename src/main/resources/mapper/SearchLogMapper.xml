<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend_novel_review.search.mapper.SearchLogMapper">

    <insert id="save">
        INSERT INTO search_logs (keyword, ip_address) VALUES (#{keyword}, #{ipAddress})
    </insert>

    <!-- 같은 IP가 같은 검색어를 최근 1시간 내 검색했는지 (조작 방지) -->
    <select id="existsRecent" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM search_logs
        WHERE keyword = #{keyword}
          AND ip_address = #{ipAddress}
          AND created_at &gt;= NOW() - INTERVAL 1 HOUR
    </select>

    <!-- 최근 7일간 검색 횟수 TOP 5 -->
    <select id="findPopular" resultType="com.example.backend_novel_review.search.dto.SearchLog">
        SELECT keyword, COUNT(*) AS count
        FROM search_logs
        WHERE created_at &gt;= NOW() - INTERVAL 7 DAY
        GROUP BY keyword
        ORDER BY count DESC, MAX(created_at) DESC
        LIMIT 5
    </select>

</mapper>
