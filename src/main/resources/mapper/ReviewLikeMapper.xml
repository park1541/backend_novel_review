<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend_novel_review.review.mapper.ReviewLikeMapper">

    <insert id="save">
        INSERT INTO review_likes (review_id, user_id) VALUES (#{reviewId}, #{userId})
    </insert>

    <delete id="delete">
        DELETE FROM review_likes WHERE review_id = #{reviewId} AND user_id = #{userId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM review_likes WHERE review_id = #{reviewId} AND user_id = #{userId}
    </select>

    <select id="countByReviewId" resultType="long">
        SELECT COUNT(*) FROM review_likes WHERE review_id = #{reviewId}
    </select>

</mapper>
