<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend_novel_review.genre.repository.GenreRepository">

    <select id="findAll" resultType="com.example.backend_novel_review.genre.domain.Genre">
        SELECT id, name FROM genres ORDER BY id
    </select>

</mapper>
